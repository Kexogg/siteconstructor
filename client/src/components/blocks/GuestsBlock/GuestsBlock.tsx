const GuestsBlock = () => {
    return (
        <div>
            <h2>Guests block</h2>
            Interactive guest (people, not companies) list.
        </div>
    );
};

export default GuestsBlock;
